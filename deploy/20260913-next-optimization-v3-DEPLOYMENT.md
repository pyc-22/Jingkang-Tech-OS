# 部署说明

1. 备份当前数据库和旧 JAR。
2. 解压 `20260913-next-optimization-v3.zip`，确认 `SHA256SUMS.txt` 校验通过。
3. 停止旧服务，替换 JAR，启动服务并检查健康端点。
4. 确认 Flyway 已执行 V92，抽查前台补单、会员余额支付和技师端操作。

## 回滚

停止新服务，恢复上一版本 JAR 与数据库备份；如 V92 已执行，保留迁移记录，旧代码将 `0` 视为不限制配置。
