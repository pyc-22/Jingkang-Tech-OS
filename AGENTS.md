# 开发协作规则

## GitHub 提交要求

每次完成一个可交付版本或修复一个 Bug 后，AI 必须在当前任务结束前完成以下操作：

1. 运行与改动相关的测试、构建或烟测，确认改动可交付。
2. 检查 Git diff，确保不包含密钥、密码、数据库备份、运行数据、构建产物或其他敏感文件。
3. 创建内容清晰、范围明确的 Git commit。
4. 将提交推送到当前仓库对应的 GitHub 远端分支。
5. 向用户报告提交哈希、远端分支和验证结果。

该规则视为用户对本仓库正常 `git commit` 和非强制 `git push` 的持续授权，无需每次重复询问。以下情况除外：

- 用户明确要求本次不要提交或不要推送。
- 测试或构建失败，改动尚未完成。
- 发现疑似敏感信息、异常远端、分支冲突或其他可能影响共享仓库的风险。
- 操作需要强制推送、跳过 Git hooks、覆盖远端历史或提交与当前任务无关的改动。

遇到例外时，AI 必须停止提交或推送，保留本地改动，并明确说明阻塞原因；不得使用 `--force`、`--no-verify` 或其他绕过方式。
