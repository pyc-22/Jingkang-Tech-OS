# 20260913-next-optimization-v1 回滚指南

## 触发条件

- 健康检查不是 `UP`，或 release 不是 `20260913-next-optimization-v1`。
- Flyway V87-V91 启动失败，或前台、店长、技师核心写操作出现阻断。
- 日报金额/营业日不一致、重复写入、锁等待异常或错误率持续升高。

## 代码与静态资源

1. 暂停入口流量和财务、房态写操作，保存日志、健康响应和 operation ID。
2. 停止当前 API/Web 服务。
3. 从部署前备份整体恢复旧版 `apps/massage-console`、JAR、外部配置和启动参数，不混用新旧静态资源。
4. 启动上一版本并确认健康号、数据库连接和派单/结算/退款关键烟测后恢复流量。

包内 `DEPLOY.ps1` 在切换失败时会从 `backup\20260913-next-optimization-v1-*` 恢复备份；自动恢复后仍需人工核对健康和业务数据。

## 数据库

V87-V91 已执行后，常规代码回滚保留新增结构和数据，旧代码应忽略未知列。不要直接删除迁移对象或修改 `flyway_schema_history`。只有在停机、审批和数据核对完成后，才可从迁移前 PostgreSQL 备份恢复数据库。

## 回滚后核对

- `/api/health`、前台、店长和技师页面返回 200。
- 派钟、房态、清洁、结算、退款、日报和历史补单记录没有半完成写入。
- 对 `PROCESSING` 离线回执逐条核对业务记录，不批量删除或重放财务操作。
- 保留本版本日志、备份路径和故障时间线。
