# 20260912-full-optimization-v1 回滚指南

## 触发条件

- 健康检查不是 `UP` 或 `release` 不是 `20260912-full-optimization-v1`。
- Flyway V90/V91 失败，或前台、店长、技师核心写操作出现新的阻断。
- 日报营业日/金额不一致、重复写入、数据库锁等待异常或错误率持续升高。

## 代码与静态资源回滚

1. 暂停入口流量和所有财务/房态写操作，保存 API/Web 日志、健康响应和 operation ID。
2. 停止当前 API/Web 服务。
3. 使用部署前备份整体恢复旧的 `apps/massage-console`、JAR、外部配置和启动参数；不要混用新旧前端资源。
4. 启动上一版本 API 和 Web，确认上一版本健康号、数据库连接和关键烟测后再恢复流量。

包内 `DEPLOY.ps1` 在切换失败时会自动从 `backup\20260912-full-optimization-v1-*` 恢复；自动恢复后仍须人工核对健康和业务数据。

## 数据库处理

V90/V91 已执行后不要直接删除表、列或修改 Flyway 历史。常规代码回滚保留新增结构和数据，旧版本应忽略未知列。若必须恢复迁移前数据，只能在停机和审批后从部署前 PostgreSQL 备份恢复，并先验证订单、退款、会员流水、提成和审计数据完整性。

## 回滚后核对

- `/api/health`、前台/店长/技师页面和静态资源返回 200。
- V87/V88/V89 状态保持成功；V90/V91 若保留则确认旧代码启动不报错。
- 抽查派单、房态、清洁、结算、退款、日报和离线回执，没有半完成写入。
- 对 `PROCESSING` 回执逐条核对对应业务记录，不批量删除或重放财务操作。
- 保留本版本日志、备份路径和故障时间线，便于后续修复。
