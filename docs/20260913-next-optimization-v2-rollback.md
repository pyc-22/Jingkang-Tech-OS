# 20260913-next-optimization-v2 回滚指南

## 触发条件

- 健康检查不是 `UP`，或 release 不是 `20260913-next-optimization-v2`。
- Flyway 启动异常，或前台、店长、技师核心写操作出现持续错误。
- 日报订单渠道、充值渠道、现金流或营业日金额不一致。
- 历史补单弹窗提交、退款、重复写入或房态更新出现异常。

## 代码与静态资源

1. 暂停流量及财务、房态写操作，保存日志、健康响应和 operation ID。
2. 停止当前 API/Web 服务。
3. 从部署前备份整体恢复旧版 `apps/massage-console`、JAR、外部配置和启动参数，不混合新旧静态资源。
4. 启动上一版本，确认健康号、数据库连接、派钟、结算、退款、充值和日报关键烟测后恢复流量。

包内 `DEPLOY.ps1` 在切换失败时会从 `backup\20260913-next-optimization-v2-*` 恢复备份；自动恢复后仍需人工核对健康和业务数据。

## 数据库

本版本充值渠道统计复用现有字段和流水，不新增迁移对象。V87-V91 已执行后，代码回滚保留新增结构和历史补单数据，旧版应忽略未知字段。不要直接删除迁移对象或修改 `flyway_schema_history`。仅在停机、数据核对和审批完成后，从迁移前 PostgreSQL 备份恢复数据库。

## 回滚后核对

- `/api/health`、前台、店长和技师页面返回 200。
- 派钟、房态、清洁、结算、退款、充值、日报和历史补单记录没有半完成写入。
- 对 `PROCESSING` 离线回执逐条核对业务记录，避免重复财务操作。
- 抽查一笔微信充值、一笔支付宝充值和一笔现金充值，确认日报渠道与现金流口径恢复一致。
- 保留本版本日志、备份路径和故障时间线。
